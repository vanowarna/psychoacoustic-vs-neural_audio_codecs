# psychoacoustic-vs-neural_audio_codecs
Comparative Analysis of Psychoacoustic vs.  Neural Audio Codecs

## Setup

Run the dependency setup script (it auto-answers apt prompts with yes):

```bash
./scripts/setup_dependencies.sh
```

If needed, make it executable first:

```bash
chmod +x scripts/setup_dependencies.sh
```
